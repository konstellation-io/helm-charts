{{- if .Values.k8sManager.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "kai.k8sManager.name" . }}
  labels:
    {{- include "opencti.knowledgeGalaxyLabels" . | nindent 4 }}
spec:
  {{- if and (not .Values.k8sManager.networkPolicy.policyTypes) (not .Values.k8sManager.networkPolicy.ingress) (not .Values.k8sManager.networkPolicy.egress) }}
  podSelector: {}
  {{- else }}
  podSelector:
    matchLabels:
      {{- include "opencti.selectorWorkerLabels" . | nindent 6 }}
  {{- end }}

  {{- if .Values.k8sManager.networkPolicy.policyTypes }}
  {{- with .Values.k8sManager.networkPolicy.policyTypes }}
  policyTypes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- else }}
  policyTypes:
    - Ingress
    - Egress
  {{- end }}

  {{- if .Values.k8sManager.networkPolicy.ingress }}
  {{- with .Values.k8sManager.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- else }}
  ingress:
  - {}
  {{- end }}

  {{- if .Values.k8sManager.networkPolicy.egress }}
  {{- with .Values.k8sManager.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- else }}
  egress:
  - {}
  {{- end }}

{{- end }}
