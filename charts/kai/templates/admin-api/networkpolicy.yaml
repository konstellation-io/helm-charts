{{- if .Values.adminApi.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "kai.adminApi.name" . }}
  labels:
    {{- include "opencti.knowledgeGalaxyLabels" . | nindent 4 }}
spec:
  {{- if and (not .Values.adminApi.networkPolicy.policyTypes) (not .Values.adminApi.networkPolicy.ingress) (not .Values.adminApi.networkPolicy.egress) }}
  podSelector: {}
  {{- else }}
  podSelector:
    matchLabels:
      {{- include "opencti.selectorWorkerLabels" . | nindent 6 }}
  {{- end }}

  {{- if .Values.adminApi.networkPolicy.policyTypes }}
  {{- with .Values.adminApi.networkPolicy.policyTypes }}
  policyTypes:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- else }}
  policyTypes:
    - Ingress
    - Egress
  {{- end }}

  {{- if .Values.adminApi.networkPolicy.ingress }}
  {{- with .Values.adminApi.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- else }}
  ingress:
  - {}
  {{- end }}

  {{- if .Values.adminApi.networkPolicy.egress }}
  {{- with .Values.adminApi.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- else }}
  egress:
  - {}
  {{- end }}

{{- end }}
